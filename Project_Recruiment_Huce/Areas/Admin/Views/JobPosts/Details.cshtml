<!--@model Project_Recruiment_Huce.Areas.Admin.Models.JobPostDetailVm
@{
    // Thiết lập tiêu đề động
    ViewBag.Title = "Chi tiết Tin tuyển dụng: " + Model.Title;
}

@Html.Partial("~/Areas/Admin/Views/Shared/_Breadcrumbs.cshtml")

<div class="row g-4">

    <div class="col-lg-8">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h5 class="card-title mb-0"><i class="bi bi-briefcase-fill"></i> Tin tuyển dụng</h5>
            </div>
            <div class="card-body">
                <h3 class="mb-3 text-primary">@Model.Title</h3>

                <div class="row border-bottom pb-3 mb-4">
                    <div class="col-md-6">
                        <div class="row mb-2"><div class="col-5 text-secondary">Mã công việc</div><div class="col-7"><strong>@Model.JobCode</strong></div></div>
                        <div class="row mb-2"><div class="col-5 text-secondary">Công ty</div><div class="col-7"><strong>@(Model.CompanyName ?? "N/A")</strong></div></div>
                        <div class="row mb-2"><div class="col-5 text-secondary">Nhà tuyển dụng</div><div class="col-7"><strong>@Model.FullName</strong> (@Model.PositionTitle)</div></div>
                        <div class="row mb-2"><div class="col-5 text-secondary">SĐT liên hệ</div><div class="col-7"><strong>@(Model.Phone ?? "N/A")</strong></div></div>
                    </div>
                    <div class="col-md-6">
                        <div class="row mb-2"><div class="col-5 text-secondary">Loại hình</div><div class="col-7"><strong>@Model.EmploymentType</strong></div></div>
                        <div class="row mb-2"><div class="col-5 text-secondary">Địa điểm</div><div class="col-7"><strong>@Model.Location</strong></div></div>
                        <div class="row mb-2"><div class="col-5 text-secondary">Hạn nộp HS</div><div class="col-7"><strong class="text-danger">@(Model.ApplicationDeadline.HasValue ? Model.ApplicationDeadline.Value.ToString("dd/MM/yyyy") : "Vô thời hạn")</strong></div></div>
                        <div class="row mb-2"><div class="col-5 text-secondary">Đăng lúc</div><div class="col-7">@(Model.PostedAt.ToString("dd/MM/yyyy") ?? "-")</div></div>
                    </div>
                </div>

                <div class="mb-4 p-3 border-start border-5 border-success bg-light rounded">
                    <h6 class="text-secondary mb-1"><i class="bi bi-cash-stack me-1"></i> Mức Lương</h6>
                    <span class="text-success fw-bold fs-5">-->
                        @* Giả định bạn có helper AdminUiHelpers.FormatMoney *@
                        <!--@(Model.SalaryFrom.HasValue ? Project_Recruiment_Huce.Areas.Admin.Models.AdminUiHelpers.FormatMoney(Model.SalaryFrom.Value) : "N/A")
                        -
                        @(Model.SalaryTo.HasValue ? Project_Recruiment_Huce.Areas.Admin.Models.AdminUiHelpers.FormatMoney(Model.SalaryTo.Value) : "N/A")
                        @Model.SalaryCurrency
                    </span>
                </div>

                <div class="mb-4">
                    <h6 class="text-secondary border-bottom pb-1 mb-2"><i class="bi bi-file-text me-1"></i> Mô tả công việc</h6>
                    <div class="p-3 bg-light rounded">
                        @Html.Raw(Model.Description ?? "-")
                    </div>
                </div>

                <div class="mb-4">
                    <h6 class="text-secondary border-bottom pb-1 mb-2"><i class="bi bi-check2-square me-1"></i> Yêu cầu công việc</h6>
                    <div class="p-3 bg-light rounded">
                        @Html.Raw(Model.Requirements ?? "-")
                    </div>
                </div>

                <div class="row mb-2">
                    <div class="col-4 text-secondary">Trạng thái</div>
                    <div class="col-8">-->
                        @* Giả định _StatusBadge.cshtml chấp nhận Model.Status *@
                        <!--@Html.Partial("~/Areas/Admin/Views/Shared/_StatusBadge.cshtml", System.Tuple.Create(Model.Status, "JobPosts"))
                    </div>
                </div>

            </div>
        </div>
    </div>

    <div class="col-lg-4">

        <div class="card shadow-sm mb-4">
            <div class="card-header bg-info text-white">
                <h6 class="mb-0"><i class="bi bi-person-lines-fill"></i> Chi tiết Yêu cầu Ứng viên</h6>
            </div>
            <div class="card-body">
                <div class="row mb-2"><div class="col-5 text-secondary">Ngành nghề</div><div class="col-7"><strong>@(Model.Industry ?? "N/A")</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Chuyên ngành</div><div class="col-7"><strong>@(Model.Major ?? "N/A")</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Kinh nghiệm</div><div class="col-7"><strong>@Model.YearsExperience năm</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Bằng cấp</div><div class="col-7"><strong>@(Model.DegreeRequired ?? "N/A")</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Số lượng</div><div class="col-7"><strong>@Model.Headcount người</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Giới tính</div><div class="col-7"><strong>@Model.GenderRequirement</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Độ tuổi</div><div class="col-7"><strong>@(Model.AgeFrom.HasValue ? Model.AgeFrom + " - " : "N/A - ")@(Model.AgeTo.HasValue ? Model.AgeTo : "N/A")</strong></div></div>
                <div class="row mb-2"><div class="col-5 text-secondary">Kỹ năng</div><div class="col-7"><span class="text-wrap small">@(Model.Skills ?? "Không có")</span></div></div>
            </div>
        </div>

        <div class="card shadow-sm">
            <div class="card-header"><h6 class="mb-0">Liên quan</h6></div>
            <div class="card-body d-grid gap-2">
                <a class="btn btn-outline-primary btn-sm" href="@Url.Action("Edit", new { id = Model.JobPostID })">
                    <i class="bi bi-pencil"></i> Chỉnh sửa tin tuyển dụng
                </a>
                <a class="btn btn-outline-primary btn-sm" href="@Url.Action("Index","Applications", new { area="Admin", q = Model.Title })">
                    <i class="bi bi-file-earmark-person"></i> Hồ sơ ứng tuyển
                </a>
            </div>
        </div>
    </div>
</div>-->