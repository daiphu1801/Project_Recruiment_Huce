@model Project_Recruiment_Huce.Areas.Admin.Models.CandidatesListVm
@{
    ViewBag.Title = "Xóa ứng viên";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@Html.Partial("~/Areas/Admin/Views/Shared/_Breadcrumbs.cshtml")

<div class="card">
  <div class="card-header bg-danger text-white">
    <h5 class="card-title mb-0"><i class="bi bi-exclamation-triangle"></i> Xác nhận xóa ứng viên</h5>
  </div>
  <div class="card-body">
    <div class="alert alert-warning">
      <strong>Cảnh báo:</strong> Bạn có chắc chắn muốn xóa ứng viên này? Hành động này không thể hoàn tác.
    </div>

    <div class="row mb-3">
      <div class="col-md-6">
        <h6>Thông tin ứng viên sẽ bị xóa:</h6>
        <table class="table table-sm">
          <tr><td><strong>ID:</strong></td><td>@Model.CandidateId</td></tr>
          <tr><td><strong>Họ tên:</strong></td><td>@(Model.FullName ?? "-")</td></tr>
          <tr><td><strong>Email:</strong></td><td>@(Model.Email ?? "-")</td></tr>
          <tr><td><strong>SĐT:</strong></td><td>@(Model.Phone ?? "-")</td></tr>
        </table>
      </div>
    </div>

    @using (Html.BeginForm("Delete", "Candidates", FormMethod.Post))
    {
      @Html.AntiForgeryToken()
      @Html.HiddenFor(m => m.CandidateId)

      <div class="d-flex justify-content-end gap-2">
        <a href="@Url.Action("Index")" class="btn btn-secondary">
          <i class="bi bi-x-circle"></i> Hủy
        </a>
        <button type="submit" class="btn btn-danger">
          <i class="bi bi-trash"></i> Xác nhận xóa
        </button>
      </div>
    }
  </div>
</div>

